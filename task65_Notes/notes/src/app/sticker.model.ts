export class Sticker {
  date: string;
  name: string;
  text: string;

  constructor(date: string, name: string, text: string){
      this.date = date;
      this.name = name;
      this.text = text;
      
  }
}
